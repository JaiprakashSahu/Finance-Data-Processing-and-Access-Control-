VITE_API_BASE_URL=http://localhost:3000
# Preferred: use JWT token from backend /auth/login
# VITE_AUTH_TOKEN=
#
# Optional backward-compat values (only useful if backend supports mock headers)
# VITE_MOCK_ROLE=admin
# VITE_MOCK_USER_ID=
